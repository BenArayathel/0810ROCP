package com.example.classes.human.models;

public class Organ {
	
	/*
	 * Even if a constructor is not specified, the JVM will provide a default constructor
	 */
	
	private String name; 	// Because we have specified it to be private, we can never specify the name of the organ 
							// A public method or at least a default method will be needed to specify a name.

}
